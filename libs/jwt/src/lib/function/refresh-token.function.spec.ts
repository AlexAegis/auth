import { doRefresh, tryRefresh } from './refresh-token.function';

describe('tryRefresh', () => {
	it('should', () => {
		expect(tryRefresh).toBeTruthy();
	});
});

describe('doRefresh', () => {
	it('should', () => {
		expect(doRefresh).toBeTruthy();
	});
});
